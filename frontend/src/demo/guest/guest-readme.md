# guest-readme

## Purpose
Guest-mode helpers that simulate data persistence without a backend connection.

## File Overview
| File / Module | Description |
| --- | --- |
| `guestMode.ts` | Controls enabling/disabling guest mode flags in local storage. |
| `guestStore.ts` | In-memory guest data store and mock API responses. |
