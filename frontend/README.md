# Frontend

React + Vite client visualizing Garmin-derived metrics using Monet-inspired styling.

## Scripts

- `npm run dev` – start Vite dev server with API proxy.
- `npm run build` – produce production build served by nginx/caddy container.
